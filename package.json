{
  "name": "adobe-animate-html",
  "version": "1.0.0",
  "private": true,
  "description": "Adobe Animate-like app built with HTML and Capacitor",
  "scripts": {
    "start": "npx cap serve",
    "android": "npx cap open android",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}